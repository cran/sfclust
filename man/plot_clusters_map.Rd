% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sfclust-methods.R
\name{plot_clusters_map}
\alias{plot_clusters_map}
\title{Plot a spatial map of cluster assignments}
\usage{
plot_clusters_map(
  x,
  sample = x$clust$id,
  clusters = NULL,
  sort = FALSE,
  legend = FALSE,
  geom_before = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{sfclust_stars} object.}

\item{sample}{Integer specifying the clustering sample to display. Defaults to the last sample.}

\item{clusters}{Optional vector of cluster IDs to include. If \code{NULL}, all clusters are shown.}

\item{sort}{Logical; if \code{TRUE}, clusters are relabeled by decreasing size. Default is \code{FALSE}.}

\item{legend}{Logical; if \code{TRUE}, a fill legend is included. Default is \code{FALSE}.}

\item{geom_before}{An optional \code{ggplot2} geom layer to add before the cluster fill layer. Default is \code{NULL}.}

\item{...}{Additional arguments passed to \code{geom_sf()}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Produces a \code{ggplot2} map of spatial regions colored by their cluster assignment for a
given MCMC sample of an \code{sfclust_stars} object.
}
