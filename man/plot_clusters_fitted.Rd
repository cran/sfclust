% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sfclust-methods.R
\name{plot_clusters_fitted}
\alias{plot_clusters_fitted}
\title{Plot functional shapes of cluster linear predictors}
\usage{
plot_clusters_fitted(
  x,
  sample = x$clust$id,
  clusters = NULL,
  sort = FALSE,
  legend = FALSE,
  inv_link = TRUE,
  fnames = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{sfclust} object.}

\item{sample}{Integer specifying the clustering sample to display. Defaults to the last sample.}

\item{clusters}{Optional vector of cluster IDs to include. If \code{NULL}, all clusters are shown.}

\item{sort}{Logical; if \code{TRUE}, clusters are relabeled by decreasing size. Default is \code{FALSE}.}

\item{legend}{Logical; if \code{TRUE}, a color legend is included. Default is \code{FALSE}.}

\item{inv_link}{Logical; if \code{TRUE} (default), values are shown on the inverse-link (mean) scale.}

\item{fnames}{Character. Name of the column to use as the x-axis (functional dimension).
If \code{NULL}, taken from the result's stored args (set automatically by \code{\link[=sfclust]{sfclust()}}).}

\item{...}{Additional arguments passed to \code{geom_line()}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plots the estimated mean functional shape (linear predictor or inverse-link scale) for
each cluster in a given MCMC sample of an \code{sfclust} object.
}
