% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-sfclust.R
\name{sfclust}
\alias{sfclust}
\alias{sfclust.default}
\alias{sfclust.data.frame}
\alias{sfclust.stars}
\title{Bayesian spatial functional clustering}
\usage{
sfclust(x, ...)

\method{sfclust}{default}(x, ...)

\method{sfclust}{data.frame}(
  x,
  adjacency,
  graphdata = NULL,
  fnames = NULL,
  nclust = 10,
  move_prob = c(0.425, 0.425, 0.1, 0.05),
  logpen = log(1 - 0.5),
  correction = TRUE,
  niter = 100,
  burnin = 0,
  thin = 1,
  nmessage = 10,
  path_save = NULL,
  nsave = nmessage,
  ...
)

\method{sfclust}{stars}(
  x,
  nclust = 10,
  graphdata = NULL,
  spnames = NULL,
  move_prob = c(0.425, 0.425, 0.1, 0.05),
  logpen = log(1 - 0.5),
  correction = TRUE,
  niter = 100,
  burnin = 0,
  thin = 1,
  nmessage = 10,
  path_save = NULL,
  nsave = nmessage,
  ...
)
}
\arguments{
\item{x}{A \code{data.frame} (core interface) or a \code{stars} object (stars interface).
Dispatch is based on this argument's class.
For \code{sfclust.data.frame}: a long-format data frame with at least columns \code{id}
(unique row index) and \code{ids} (integer spatial unit index, 1 to \code{ns}), plus
any response and covariate columns referenced in \code{formula}.
For \code{sfclust.stars}: a \code{stars} object containing response variables, covariates,
and other necessary data.}

\item{...}{Additional arguments such as \code{formula}, \code{family}, and others passed to
\code{inla()}.}

\item{adjacency}{A square weighted adjacency matrix (ns × ns) encoding spatial
contiguity and edge weights. Can be a dense \code{matrix} or a sparse \code{Matrix}.
Typically obtained via \code{\link[igraph:as_adjacency_matrix]{igraph::as_adjacency_matrix()}} on the graph returned
by \code{\link[=genclust]{genclust()}}.}

\item{graphdata}{A list with components \code{graph}, \code{mst}, and \code{membership} as returned
by \code{\link[=genclust]{genclust()}}. If \code{NULL}, it is built automatically.}

\item{fnames}{Character. Name of the column in \code{x} that holds the functional
index (e.g. \code{"id_time"}). Used by plot methods; not required by the
algorithm itself. Default is \code{NULL}.}

\item{nclust}{Integer. Initial number of clusters when \code{graphdata = NULL}. Ignored
if \code{graphdata} is provided (default is \code{10}).}

\item{move_prob}{A numeric vector of probabilities for the MCMC move types: birth,
death, change, and hyperparameter (default is \code{c(0.425, 0.425, 0.1, 0.05)}).}

\item{logpen}{A negative numeric value representing the log-scale penalty for
increasing the number of clusters by one. The number of clusters is assumed to
follow a geometric prior with probability \code{q}, making this penalty equal to
\code{log(1 - q)}. For example, if \code{logpen = -50}, then a proposal that increases
the number of clusters will only be favored if it improves the log marginal
likelihood by more than 50.}

\item{correction}{A logical indicating whether correction to compute the marginal
likelihoods should be applied (default is \code{TRUE}). This depends on the type
of effects included in the \code{INLA} model.}

\item{niter}{An integer specifying the number of MCMC iterations after burn-in
(default is \code{100}).}

\item{burnin}{An integer specifying the number of burn-in iterations to discard
(default is \code{0}).}

\item{thin}{An integer specifying the thinning interval for recording the results
(default is \code{1}).}

\item{nmessage}{An integer specifying how often progress messages should be printed
(default is \code{10}).}

\item{path_save}{A character string specifying the file path to save the results
(default is \code{NULL}).}

\item{nsave}{An integer specifying the number of iterations between saved results
(default is \code{nmessage}).}

\item{spnames}{Character vector with the names of the spatial dimensions of \code{x}.
If \code{NULL} (default), auto-detected from the \code{stars} object dimensions.}
}
\value{
An \code{sfclust} object (from \code{sfclust.data.frame}) or an \code{sfclust_stars} object
inheriting from \code{sfclust} (from \code{sfclust.stars}). Both contain:
\itemize{
\item \code{samples}: MCMC trace with \code{membership}, \code{log_mlike}, and \code{move_counts}.
\item \code{clust}: selected clustering with \code{id}, \code{membership}, and fitted \code{models}.
}

\code{sfclust_stars} additionally carries \code{input_args} with \code{stars} (structural shell
of the input), \code{spnames}, and \code{fnames}, used by spatial plot methods.
}
\description{
\code{sfclust()} is the main user-facing function for Bayesian spatial functional
clustering via reversible-jump MCMC. It dispatches on the class of the first
argument:
}
\details{
\itemize{
\item \code{sfclust.data.frame()}: core interface — takes a pre-built long-format data frame
and a weighted adjacency matrix. Use this when working with any data format
after converting it yourself.
\item \code{sfclust.stars()}: stars wrapper — takes a \code{stars} spatio-temporal object,
converts it to long format, builds the spatial graph, and calls the core algorithm.
}

This implementation draws inspiration from the methods described in the paper:
\emph{"Bayesian Clustering of Spatial Functional Data with Application to a Human Mobility
Study During COVID-19"} by Bohai Zhang, Huiyan Sang, Zhao Tang Luo, and Hui Huang,
published in \emph{The Annals of Applied Statistics}, 2023. For further details on the
methodology, please refer to:
\itemize{
\item The paper: \doi{10.1214/22-AOAS1643}
\item Supplementary material: \doi{10.1214/22-AOAS1643SUPPB}
}

The MCMC algorithm in this implementation is largely based on the supplementary
material provided in the paper. However, we have generalized the computation of the
marginal likelihood ratio by leveraging INLA (Integrated Nested Laplace Approximation).
This generalization enables integration over all parameters and hyperparameters,
allowing for inference within a broader family of distribution functions and model
terms, thereby extending the scope and flexibility of the original approach.
Further details of our approach can be found in our paper \emph{"Bayesian spatial functional
data clustering: applications in disease surveillance"} by Ruiman Zhong, Erick A.
Chacón-Montalván, Paula Moraga:
\itemize{
\item The paper: \doi{10.1002/sim.70597}
}
}
\examples{

\donttest{
if (requireNamespace("INLA", quietly = TRUE)) {
library(sfclust)

# Stars interface: Gaussian model
data(stgaus)
result <- sfclust(stgaus, formula = y ~ f(id_time, model = "rw1"),
  niter = 10, nmessage = 1)

print(result)
summary(result, sort = TRUE)
fitted(result, sort = TRUE)

plot(result)
plot_clusters_series(result, var = y)

result2 <- update(result, niter = 2, nmessage = 1)
plot(result2)

# Stars interface: Binomial model
data(stbinom)
result <- sfclust(stbinom, formula = cases ~ poly(id_time, 2) + f(id),
  family = "binomial", Ntrials = population, niter = 10, nmessage = 1)

print(result)
summary(result, sort = TRUE)
fitted(result, sort = TRUE)

plot(result)
plot_clusters_series(result, var = cases/population)

result2 <- update(result, niter = 2, nmessage = 1)
plot(result2)

# data.frame interface: Poisson model
 ns <- 6L; nt <- 4L
 set.seed(4)
 df  <- data.frame(
   id       = seq_len(ns * nt),
   ids      = rep(seq_len(ns), nt),
   id_time  = rep(seq_len(nt), each = ns),
   expected = rep(10L, ns * nt)
 )
 df <- transform(df,
   y = rpois(ns * nt, expected * exp(0.5 * id_time * rep(c(-1, 1), each = ns / 2, nt)))
 )
 adj <- Matrix::sparseMatrix(
   i = c(1, 2, 4, 5, 1, 2, 3), j = c(2, 3, 5, 6, 4, 5, 6),
   x = 1L, dims = c(ns, ns), symmetric = TRUE
 )

 result <- sfclust(df, adjacency = adj, nclust = 3, fnames = "id_time",
   formula = y ~ 1 + id_time, family = "poisson", E = expected,
   niter = 3, burnin = 0, thin = 1, nmessage = 1)

 print(result)
 summary(result, sort = TRUE)
 fitted(result, sort = TRUE)

 plot(result)
 plot_clusters_series(result, var = y)

 result2 <- update(result, niter = 2, nmessage = 1)
 plot(result2)
}
}

}
\author{
Ruiman Zhong \email{ruiman.zhong@kaust.edu.sa},
Erick A. Chacón-Montalván \email{erick.chaconmontalvan@wur.nl},
Paula Moraga \email{paula.moraga@kaust.edu.sa}
}
